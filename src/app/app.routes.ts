import { Routes } from '@angular/router';
// import { App } from './app';
import { AuthPage } from './auth-page/auth-page';

export const routes: Routes = [
    {
        path: '',
        component: AuthPage,
    },
];
